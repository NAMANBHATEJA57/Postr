{
    "version": 2,
    "buildCommand": "cd server && npx prisma generate && cd .. && npm run build --workspaces --if-present",
    "installCommand": "npm install",
    "builds": [
        {
            "src": "client/package.json",
            "use": "@vercel/next"
        },
        {
            "src": "server/src/index.ts",
            "use": "@vercel/node"
        }
    ],
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/server/src/index.ts"
        },
        {
            "source": "/(.*)",
            "destination": "/client/$1"
        }
    ]
}