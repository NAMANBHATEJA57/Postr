import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const data = { id: 'test_bug_fix_02', mediaUrl: 'test', mediaType: 'image', title: 'Test', message: 'Test msg', toName: '', fromName: '', theme: 'framed', expiryAt: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000) }; await prisma.postcard.create({ data }); const p = await prisma.postcard.findUnique({ where: { id: 'test_bug_fix_02' } }); console.log('Created:', p.createdAt); console.log('Expiry:', p.expiryAt); await prisma.postcard.delete({ where: { id: 'test_bug_fix_02' } }); } main().then(()=>prisma.$disconnect());
